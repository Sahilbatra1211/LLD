#pragma once

#include <bits/stdc++.h>

using namespace std;

// this service will have a publisher we are taking a single publisher for now
// or we can decide the publisher based on notificaiton type and we will use observer pattern here
// suppose notifciaiton type is something specific like CUSTOMER_NOTIFICATION
// so we will have the publisher for it and that will contain the observer list who will subscribe to that publisher
class NotificationService {

public:
    
};