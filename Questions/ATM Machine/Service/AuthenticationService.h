#pragma once

#include <bits/stdc++.h>
#include "../Entity/Card.h"
using namespace std;

class AuthenticationService {
public:
    bool checkPin(Card* card,int pin){
        return true;
    }
    
};